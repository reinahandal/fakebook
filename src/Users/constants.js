// We would prefer to use the singular naming convention for ENUM Like constants. So it should be ROUTE
export const ROUTES = {
    ROOT: `/users`,
}


// This could be optimized by using a single query key for both
export const QUERY_KEY = {
    USERS: `users`,
    SINGLE: `user`,
}